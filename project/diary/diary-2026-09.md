# 2026-09

## 2026-09-18
- idea - make it a "vacation trip website" generator
- So not just generating "maps" of cities
  - fill in a "trip profile"
    - dates
    - locations
    - events
    - see: project/ideas/mexico-city-2026.md

- the the application "builds" a static site that is easy to share with people on the trip
- the core of the site would be a map of the "city"
- key locations on the map would be pulled from the config
- In addition to the map, the site could have sections
  - a list of popular locations in the city - check boxes that turn on/off display on the map
  - a list of activities in the city during the dates of the trip
- once the site is generated and iterated on locally, an "export" button would create the site as a static HTML that could be easily shared (e.g. AWS S3 site or CLoudFlare)


## 2026-09-19
Alice here. `localhost` only works on the Mac itself, so the iPhone needs your Mac's address on your home Wi-Fi.

**On the real iPhone (same Wi-Fi as the Mac)**
1. Stop the current server with Ctrl-C, then restart it open to your local network:
   ```
   python -m http.server -d dist 8000 --bind 0.0.0.0
   ```
   If macOS asks whether Python can accept incoming connections, click **Allow**.
2. On the iPhone, open Safari and go to **http://<mac-ip>:8000/<slug>/**
3. Press Ctrl-C when you're done. While it runs, anyone on your Wi-Fi can open the page.

If it doesn't load, check that both devices are on the same network and that the Mac's firewall isn't blocking Python. Also, `<mac-ip>` can change after a reboot; `ipconfig getifaddr en0` gives the current one.

**Quick look without the phone**
- **Safari on the Mac:** Develop › Enter Responsive Design Mode, then pick an iPhone. If the Develop menu is hidden, turn on Safari › Settings › Advanced › "Show features for web developers".
- **Chrome:** open DevTools with Cmd-Opt-I, turn on the device toolbar with Cmd-Shift-M, then pick an iPhone.

The real phone is worth doing once: it's the only way to check touch, especially whether dragging moves the map or scrolls the page. Quincy couldn't test that.

## 2026-09-21
Alice here. Big session: tripsite v0 built, verified and shipped, then the project renamed.

**Built — `tripsite/`, a CLI that turns a trip profile into a static site**
- `uv run tripsite/build.py trips/<file>.md` → `dist/<slug>/index.html` (self-contained),
  plus `dist/index.html`, `404.html`, `robots.txt`, `_headers`.
- Trip profile = Markdown + YAML frontmatter in gitignored `trips/`. Schema: trip, privacy,
  airport, map, locations, popular, events.
- Map (Leaflet + OSM tiles): our places always on, popular places toggleable per place and
  per category, airport marker, Metro lines (Overpass, cached, off by default), events by
  day, "Just outside your dates" section for `keep: true` events, `url:` links on places
  and events opening in a new tab.
- Privacy: `hotel_display: exact | approximate | hidden`. Approximate draws a 400 m circle
  around an offset centre ~150–250 m from the true point; a leak scan fails the build if
  the stay's name, street, postcode, url or 4-dp coords reach the output.
- 164 tests (7 expectedFailures = accepted limits, documented in tripsite/README.md).

**Renamed maptoposter → worldcities** (repo, folder, docs, venv, memory). Upstream credit
to originalankur/maptoposter kept; `upstream` remote added — check with
`git log --oneline HEAD..upstream/main`. Runbook: `project/RENAME.md`.

**State at session end**
- `main` = rename commit, pushed. Working tree clean.
- Trip slug regenerated → `mexico-city-2026-6b9638` (the old one leaked into public commits).
- `hotel_display` is currently `exact` — decide before deploying.
- Nothing deployed. Cloudflare gates 1–6 in `tripsite/README.md` are all still to run;
  Gate 4 (Zero Trust) needs a card on file even at $0.

**Suggested prompt for next session**

> Alice — worldcities. Read project/diary/diary-2026-09.md (2026-09-21) and
> tripsite/README.md. I want to deploy the Mexico City trip site to Cloudflare Pages on
> worldcities.ca. Walk me through the deploy gates one at a time: 1 (zone check), 2
> (placeholder upload), 3 (custom domain), 3b (pages.dev lockdown), 4 (Access + email OTP
> for my trip-mates), 5 (upload the trip). Before gate 5, confirm with me whether the hotel
> should be exact or approximate. I'll run the dashboard and wrangler steps; you verify
> each one before we move on.

Alternative next sessions, if deploying isn't the priority:
- "Alice — check the trip site on my iPhone and fix anything that's wrong on touch"
  (Quincy never tested real touch: map drag vs page scroll, the small ↗ link targets).
- "Alice — add a second trip to worldcities and make sure both can be live at once"
  (an upload replaces the whole site; every live trip must be rebuilt in the same run).
- "Alice — generate a poster for Mexico City and put it on the trip page as a banner"
  (the poster engine is still there and unused by tripsite).


**SPECIAL NOTE**
- there was an old repo called "worldcities".  No work every done - just an idea.
- I copied that info to this repo as it is related:
  - project/diary/diary-2025-12.md
  - project/ideas
- those thoughts can stay in background for now






----

Yes. Two commands. Get the zone ID, then dump every record:

# 0. Login
aws login --profile rob


# 1. Zone ID
aws route53 list-hosted-zones-by-name --dns-name worldcities.ca \
  --query "HostedZones[?Name=='worldcities.ca.'].{Id:Id,Name:Name,Records:ResourceRecordSetCount}" \
  --output table --profile rob


--------------------------------------------------------------------
|                       ListHostedZonesByName                      |
+-----------------------------------+-------------------+----------+
|                Id                 |       Name        | Records  |
+-----------------------------------+-------------------+----------+
|  /hostedzone/Z0944732VRZ4NUBNE0FL |  worldcities.ca.  |  5       |
+-----------------------------------+-------------------+----------+



# 2. Full record export -> OUTSIDE the repo
ZID=<paste the id, the part after /hostedzone/>

ZID=Z0944732VRZ4NUBNE0FL
aws route53 list-resource-record-sets --hosted-zone-id "$ZID" \
  --output json > ./project/temp/worldcities.ca-route53-2026-09-21.json --profile rob

# 3. Human-readable, for diffing against Cloudflare's import
aws route53 list-resource-record-sets --hosted-zone-id "$ZID" --profile rob \
  --output text | sort > ./project/temp/worldcities.ca-route53-2026-09-21.txt




bella.ns.cloudflare.com
gabriel.ns.cloudflare.com

## 2026-09-21 (evening) — deploy session: domain migrated, trip not yet live

Alice here. Second session today. Session A and Session B of the deploy both ran; the
domain moved from AWS to Cloudflare with live mail intact.

**The plan changed at gate 1.** The starting prompt assumed `worldcities.ca` was already a
Cloudflare zone. It wasn't — Route 53, apex and `www` aliased to a 2020 S3 placeholder, and
**three live Zoho MX records**. That turned a 5-gate deploy into a domain migration with
real email in the blast radius, and added the Gate 0 family (0a–0g).

**Ran and passed, in order:** Gate 2 (Pages project, placeholder only, real 404 — hash
`2ca4e8dc`) · Gate 3b (`*.pages.dev` → `worldcities.ca`, hash previews included) · 0a
(authoritative Route 53 export, 5 record sets, backed up twice) · 0b (MX TTL already 300 —
no action) · 0c (Cloudflare zone, exactly 3 MX, pre-flip dig green from `bella` and
`gabriel`) · 0e baseline · **0d the cutover — registry flipped 19:10:10** · 0e post-flip ·
Gate 1 (zone Active, baseline export taken).

**Mail never broke.** Four resolvers, three Zoho MX, unchanged throughout. The mechanism
wasn't the rollback card — it was putting identical MX on both providers before the flip, so
it stopped mattering which nameserver a resolver asked. The number that governs the cutover
is the parent `.ca` delegation TTL of **86400**, measured at a `.ca` TLD server; the MX TTL
(300) and the in-zone NS TTL are both decoys.

**Zero Trust onboarded** — Free, 2 of 50 seats, team `worldcities-trips`, One-time PIN as the
only login method. It does route through a $0 billing checkout and takes a card; Cloudflare's
own order summary says usage past 50 seats is billed, not blocked.

**Five defects found in the gate cards, by running them.** Two were expensive: the BIND
greps reported 4 and 1 on a correct zone (they were counting the export's own comment text),
and the mail gate demanded a literal `mx.zoho.com` when the real receiving MTA is
`mx.zohomail.com`. Both would have produced a false verdict at a moment whose documented
reaction was a rollback. Full list in DECISIONS.md.

**Protocol files added this session:** PROJECT.md, PLAN.md, STATE.md, TASKS.md, DECISIONS.md,
from the AI-ProjectStartup template. STATE.md is the file to read first.

**One thing blocks the rest:** the Gate 0e 24-hour mail re-test, **earliest 2026-09-22 19:10
MDT**. Last night's pass could have been answered by a resolver still holding the AWS
delegation; tomorrow's can only have gone via Cloudflare. And the split window is measured,
not theoretical — at 19:40 `1.1.1.1` and `9.9.9.9` still served the old S3 records while
`8.8.8.8` had switched, which is exactly why Gate 3 waits.

**Suggested prompt for next session**

> Alice — worldcities. Read STATE.md and TASKS.md. Run the Gate 0e 24-hour mail re-test
> first, then continue Session C: Gates 3, 4 and 5 to put the Mexico City trip live at
> worldcities.ca for me and Lucie. I need to decide `hotel_display` before Gate 5 — remind
> me. I'll run the dashboard and wrangler steps; you verify each one before we move on.


## 2026-09-22
- publish to Cloudflare and worldcities.ca


Pearl's draft for Lucie is ready. She suggests two short texts rather than one: the first reads like a message from you, the second has the login details.

**Text 1:**
> Hey Lucie! Made us a little site for Mexico City — map, places, events, where we're staying: https://worldcities.ca/mexico-city-2026-6b9638/

**Text 2:**
> Log in with Lucie's Gmail (project/secrets/access-allowlist.txt) (that exact address, it's the only one that works) — you'll get a one-time PIN emailed from noreply@notify.cloudflare.com, check spam, it expires in 10 min. Heads up, the page says "code emailed" even if the address is wrong, so if nothing arrives that's the first thing to check. Stays logged in about a month after that — good through the trip.

She also wrote a single-text version and an email version (subject "Our Mexico City site", with a `[sign-off]` for you to fill in). Say if you'd rather have one of those.

Once Lucie confirms she's in, I'll count her address on the allowlist as confirmed.

**Next:** the mail re-test at 19:10 MDT. Send Gmail → `rob@worldcities.ca`, save "Show original" under a new filename, and send me the path.

**Session C close (Alice, 16:40 MDT)** — **Mexico City trip live** at
https://worldcities.ca/mexico-city-2026-6b9638/. Gates 3, 4, 5 passed; Rob's login works.
Ran ahead of the 24-h re-test on Rob's call (every resolver had switched). `hotel_display`
= exact. Gate 4 defect 6 found (404 false pass). 3 DECISIONS entries written.
Rob to do before next session: 0e re-test (after 19:10), send Lucie the link + confirm
her login, iPhone check.

**Suggested prompt for next session**

> Alice — worldcities. Read STATE.md and TASKS.md. I've done the 0e mail re-test (the
> .eml is at <path>), Lucie has/hasn't logged in, and here's what I saw on the iPhone:
> <notes>. Verify the .eml, then have Gage amend Gate 4's verify (defect 6) and tidy the
> stale 24-hour preconditions, decide Gate 0f (www), and close the milestone if everything's green.
